ktor {
    deployment {
        port = 9000
        host = "0.0.0.0"
    }
    application {
        modules = [ hylosy.pcea.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:mysql://localhost:3306/pcea?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo&rewriteBatchedStatements=true"
    driver = "com.mysql.cj.jdbc.Driver"
    user = "root"
    password = "root"
}